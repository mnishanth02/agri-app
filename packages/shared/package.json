{
  "name": "@viz-crop/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types, zod schemas, and pure utilities for @viz-crop/web and @viz-crop/api.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@turf/area": "^7.3.5",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
